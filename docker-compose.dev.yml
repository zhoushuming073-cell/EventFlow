# 本地开发 override：暴露 MySQL 3306 和 FastAPI 8000 到宿主机，方便调试。
# 用法：docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
# 生产环境只用 docker-compose.yml，不要带这个文件。
services:
  mysql:
    ports:
      - "3306:3306"

  server:
    environment:
      ENV: dev
    ports:
      - "8000:8000"
